function CompleteComp({ continueToNextSet }) {
    return (
        <div>
            <h1>Displaying complete comp!</h1>
            <button onClick={continueToNextSet}>Continue to the next set!</button>
        </div>
    );
}

export default CompleteComp;